# Stack

La pila es una estructura de datos mas importante porque enseña una forma diferente de organizar informacion.
Usan el concepto LIFO (Last in, First out) ultimo en entrar primero en salir.
Tiene operaciones como push(Agregar elemento), pop(Eliminar elemento de la cima) y peek(ver elemento sin eliminar).
