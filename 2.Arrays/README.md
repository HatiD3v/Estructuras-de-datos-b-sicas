# Arrays

Son una estructura de datos que almacenan valores en orden, a cada valor se accede mediante un indice que indica su posicion.
Siempre los indices comenzaran en 0 indicando el primer valor y se guardan en la region de memoria llamada HEAP
